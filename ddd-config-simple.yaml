settings:
  preset: "simple"

layers:
  domain:
    entities: User
    value_objects: Email
  application:
    exceptions: UserNotFoundException
  infrastructure:
    repositories: UserRepository
  interface:
    controllers: UserController
